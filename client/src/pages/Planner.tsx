/** Monsoon Atlas planner: a six-stop travel dossier that makes preferences feel like wayfinding choices. */
import { ArrowLeft, ArrowRight, Check, CircleDollarSign, Compass, MapPin, Minus, Plus, Sparkles, UsersRound } from "lucide-react";
import { useMemo, useState } from "react";
import { useLocation } from "wouter";
import SiteHeader from "@/components/SiteHeader";
import RouteMotif from "@/components/RouteMotif";
import { INDIAN_DESTINATIONS, type Interest, type PlanInputs } from "@/lib/tripData";
import { useTrip } from "@/contexts/TripContext";

const labels = ["Destination", "Dates", "Travellers", "Budget", "Interests", "Pace"];
const interests: Interest[] = ["Nature", "Food", "History", "Adventure", "Shopping", "Nightlife", "Photography", "Culture", "Beaches", "Architecture", "Family"];
const styleCopy = { Relaxed: "Fewer stops, longer pauses and time to follow a whim.", Balanced: "The best of both: anchors, air and a few unexpected turns.", Packed: "A fuller route for travellers who like to see it all." };

function CountControl({ label, value, onChange, detail }: { label: string; value: number; onChange: (value: number) => void; detail: string }) { return <div className="flex items-center justify-between rounded-2xl border border-[#123a35]/12 bg-[#fffdf8] p-4"><div><p className="font-bold">{label}</p><p className="mt-1 text-sm text-[#6b827c]">{detail}</p></div><div className="flex items-center gap-3"><button aria-label={`Decrease ${label}`} onClick={() => onChange(Math.max(0, value - 1))} className="grid h-8 w-8 place-items-center rounded-full border border-[#123a35]/15 text-[#123a35]"><Minus className="h-3.5 w-3.5" /></button><span className="w-4 text-center font-mono font-bold">{value}</span><button aria-label={`Increase ${label}`} onClick={() => onChange(value + 1)} className="grid h-8 w-8 place-items-center rounded-full bg-[#123a35] text-white"><Plus className="h-3.5 w-3.5" /></button></div></div>; }

export default function Planner() {
  const { plan, forgeTrip } = useTrip(); const [, setLocation] = useLocation(); const [step, setStep] = useState(0); const [draft, setDraft] = useState<PlanInputs>(plan); const [error, setError] = useState("");
  const progress = useMemo(() => Math.round(((step + 1) / labels.length) * 100), [step]);
  const next = () => { if (step === 0 && !draft.destination.trim()) return setError("Choose a destination to start shaping your route."); if (step === 1 && (!draft.startDate || !draft.endDate || draft.endDate <= draft.startDate)) return setError("Choose an end date after your start date."); setError(""); if (step < labels.length - 1) setStep(step + 1); else { forgeTrip(draft); setLocation("/itinerary"); } };
  const toggleInterest = (interest: Interest) => setDraft((current) => ({ ...current, interests: current.interests.includes(interest) ? current.interests.filter((item) => item !== interest) : [...current.interests, interest] }));
  const content = [
    <div key="destination" className="space-y-6"><div><p className="atlas-label text-[#e6651b]">Step 01 · Begin here</p><h1 className="mt-4 font-display text-4xl tracking-[-.06em] sm:text-5xl">Where are you going?</h1><p className="mt-3 text-lg leading-7 text-[#5d7771]">Start with a place. We’ll take care of turning it into a route with rhythm.</p></div><label className="grid gap-2 text-sm font-bold">Destination<select value={draft.destination} onChange={(event) => setDraft({ ...draft, destination: event.target.value })} className="rounded-2xl border border-[#123a35]/15 bg-[#fffdf8] px-4 py-4 text-base font-medium outline-none ring-[#e6651b] focus:ring-2">{INDIAN_DESTINATIONS.map((destination) => <option value={destination.name} key={destination.name}>{destination.name}, {destination.region}</option>)}</select></label><label className="grid gap-2 text-sm font-bold">Starting from<div className="relative"><MapPin className="pointer-events-none absolute left-4 top-1/2 h-4 w-4 -translate-y-1/2 text-[#e6651b]" /><input value={draft.startLocation} onChange={(event) => setDraft({ ...draft, startLocation: event.target.value })} placeholder="e.g. Delhi" className="w-full rounded-2xl border border-[#123a35]/15 bg-[#fffdf8] py-4 pl-11 pr-4 text-base font-medium outline-none focus:ring-2 focus:ring-[#e6651b]" /></div></label><div className="rounded-2xl bg-[#e4f0e8] p-4 text-sm leading-6 text-[#345d55]"><Compass className="mr-2 inline h-4 w-4 text-[#e6651b]" /> We’ll use your departure point to make the transport estimate more useful.</div></div>,
    <div key="dates" className="space-y-6"><div><p className="atlas-label text-[#e6651b]">Step 02 · Set the horizon</p><h1 className="mt-4 font-display text-4xl tracking-[-.06em] sm:text-5xl">When are you going?</h1><p className="mt-3 text-lg leading-7 text-[#5d7771]">Pick a window and we’ll shape the days in between.</p></div><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-bold">Start date<input type="date" value={draft.startDate} onChange={(event) => setDraft({ ...draft, startDate: event.target.value })} className="rounded-2xl border border-[#123a35]/15 bg-[#fffdf8] px-4 py-4 text-base font-medium outline-none focus:ring-2 focus:ring-[#e6651b]" /></label><label className="grid gap-2 text-sm font-bold">End date<input type="date" value={draft.endDate} onChange={(event) => setDraft({ ...draft, endDate: event.target.value })} className="rounded-2xl border border-[#123a35]/15 bg-[#fffdf8] px-4 py-4 text-base font-medium outline-none focus:ring-2 focus:ring-[#e6651b]" /></label></div></div>,
    <div key="travellers" className="space-y-6"><div><p className="atlas-label text-[#e6651b]">Step 03 · Your company</p><h1 className="mt-4 font-display text-4xl tracking-[-.06em] sm:text-5xl">Who’s going?</h1><p className="mt-3 text-lg leading-7 text-[#5d7771]">A good route accounts for the people moving through it.</p></div><div className="grid gap-3"><CountControl label="Adults" value={draft.adults} onChange={(adults) => setDraft({ ...draft, adults })} detail="Ages 13 and over" /><CountControl label="Children" value={draft.children} onChange={(children) => setDraft({ ...draft, children })} detail="Ages 2–12" /></div><div className="rounded-2xl bg-[#f2eddf] p-4 text-sm font-semibold text-[#486962]"><UsersRound className="mr-2 inline h-4 w-4 text-[#e6651b]" /> Travelling as a group? We’ll keep route timings practical.</div></div>,
    <div key="budget" className="space-y-6"><div><p className="atlas-label text-[#e6651b]">Step 04 · Give it a shape</p><h1 className="mt-4 font-display text-4xl tracking-[-.06em] sm:text-5xl">What’s your budget?</h1><p className="mt-3 text-lg leading-7 text-[#5d7771]">A realistic budget lets the right kinds of moments rise to the top.</p></div><label className="grid gap-2 text-sm font-bold">Total trip budget<div className="relative"><CircleDollarSign className="pointer-events-none absolute left-4 top-1/2 h-5 w-5 -translate-y-1/2 text-[#e6651b]" /><input type="number" min="5000" value={draft.budget} onChange={(event) => setDraft({ ...draft, budget: Number(event.target.value) })} className="w-full rounded-2xl border border-[#123a35]/15 bg-[#fffdf8] py-4 pl-11 pr-4 text-xl font-bold outline-none focus:ring-2 focus:ring-[#e6651b]" /></div></label><div className="grid grid-cols-3 gap-3">{(["Budget", "Balanced", "Premium"] as const).map((level) => <button key={level} onClick={() => setDraft({ ...draft, budgetLevel: level })} className={`rounded-2xl border p-4 text-left transition-all ${draft.budgetLevel === level ? "border-[#123a35] bg-[#123a35] text-white shadow-lg" : "border-[#123a35]/12 bg-[#fffdf8] text-[#496861] hover:border-[#e6651b]"}`}><span className="block text-sm font-bold">{level}</span><span className={`mt-1 block text-xs ${draft.budgetLevel === level ? "text-white/70" : "text-[#79918b]"}`}>{level === "Budget" ? "Savvy finds" : level === "Balanced" ? "Comfortable picks" : "Extra ease"}</span></button>)}</div></div>,
    <div key="interests" className="space-y-6"><div><p className="atlas-label text-[#e6651b]">Step 05 · The good stuff</p><h1 className="mt-4 font-display text-4xl tracking-[-.06em] sm:text-5xl">What do you enjoy?</h1><p className="mt-3 text-lg leading-7 text-[#5d7771]">Choose a few true interests. The route will favour them, not just name-check them.</p></div><div className="flex flex-wrap gap-3">{interests.map((interest) => { const active = draft.interests.includes(interest); return <button key={interest} onClick={() => toggleInterest(interest)} className={`rounded-full border px-4 py-2.5 text-sm font-bold transition-all ${active ? "border-[#e6651b] bg-[#e6651b] text-white" : "border-[#123a35]/15 bg-[#fffdf8] text-[#42655d] hover:border-[#e6651b]"}`}>{active && <Check className="mr-1.5 inline h-3.5 w-3.5" />}{interest}</button>; })}</div><p className="text-sm text-[#6d867f]">{draft.interests.length ? `${draft.interests.length} interests selected` : "Pick at least one for a more personal route."}</p></div>,
    <div key="pace" className="space-y-6"><div><p className="atlas-label text-[#e6651b]">Step 06 · Your tempo</p><h1 className="mt-4 font-display text-4xl tracking-[-.06em] sm:text-5xl">How do you like to move?</h1><p className="mt-3 text-lg leading-7 text-[#5d7771]">This decision sets the breathing room between your anchor moments.</p></div><div className="grid gap-3">{(["Relaxed", "Balanced", "Packed"] as const).map((style) => <button key={style} onClick={() => setDraft({ ...draft, travelStyle: style })} className={`group flex items-center justify-between rounded-2xl border p-5 text-left transition-all ${draft.travelStyle === style ? "border-[#123a35] bg-[#123a35] text-white" : "border-[#123a35]/12 bg-[#fffdf8] hover:border-[#e6651b]"}`}><div><p className="font-display text-2xl tracking-[-.04em]">{style}</p><p className={`mt-1 max-w-md text-sm leading-6 ${draft.travelStyle === style ? "text-white/70" : "text-[#69827c]"}`}>{styleCopy[style]}</p></div><span className={`grid h-8 w-8 place-items-center rounded-full ${draft.travelStyle === style ? "bg-[#e6651b] text-white" : "border border-[#123a35]/12 text-[#e6651b]"}`}>{draft.travelStyle === style ? <Check className="h-4 w-4" /> : <ArrowRight className="h-4 w-4" />}</span></button>)}</div></div>,
  ][step];
  return <div className="min-h-screen bg-[#f7f2e7] text-[#123a35]"><SiteHeader /><main className="mx-auto grid max-w-[1440px] gap-8 px-5 py-7 lg:grid-cols-[270px_minmax(0,760px)] lg:gap-16 lg:px-10 lg:py-14"><aside className="relative lg:sticky lg:top-24 lg:h-fit"><RouteMotif className="absolute -left-12 top-7 hidden h-[330px] w-[145px] opacity-45 lg:block" /><div className="relative"><LinkBack /><div className="mt-7 hidden lg:block"><p className="atlas-label text-[#e6651b]">Your travel dossier</p><p className="mt-2 max-w-[210px] text-xs leading-5 text-[#71877f]">Every decision becomes a waypoint in a route built around you.</p><ol className="mt-6 grid gap-0">{labels.map((label, index) => <li key={label} className={`relative flex items-center gap-3 py-3 text-sm font-bold ${index === step ? "text-[#e6651b]" : index < step ? "text-[#486962]" : "text-[#9baeaa]"}`}><span className={`grid h-7 w-7 place-items-center rounded-full text-xs ${index === step ? "bg-[#e6651b] text-white" : index < step ? "bg-[#dcebe3] text-[#123a35]" : "border border-[#123a35]/15"}`}>{index < step ? <Check className="h-3.5 w-3.5" /> : index + 1}</span>{label}</li>)}</ol></div><div className="mt-5 lg:hidden"><div className="flex items-center justify-between"><span className="atlas-label">Route point {step + 1} of 6</span><span className="text-xs font-bold text-[#e6651b]">{labels[step]}</span></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-[#123a35]/10"><div className="h-full rounded-full bg-[#e6651b] transition-all duration-300" style={{ width: `${progress}%` }} /></div></div></div></aside><section className="relative overflow-hidden rounded-[2rem] border border-[#123a35]/5 bg-[#fffdf8] p-6 shadow-[0_18px_46px_-32px_rgba(18,58,53,.38)] sm:p-9"><RouteMotif className="absolute -right-8 top-2 h-[260px] w-[125px] opacity-30" /><div className="relative"><div className="hidden lg:block"><div className="flex items-center justify-between gap-5"><div className="h-1.5 flex-1 overflow-hidden rounded-full bg-[#123a35]/8"><div className="h-full rounded-full bg-[#e6651b] transition-all duration-300" style={{ width: `${progress}%` }} /></div><span className="atlas-label whitespace-nowrap text-[#e6651b]">Route point {String(step + 1).padStart(2, "0")}</span></div></div><div className="mt-9">{content}</div>{error && <p role="alert" className="mt-6 rounded-xl bg-[#fff0e5] px-4 py-3 text-sm font-semibold text-[#bd4a10]">{error}</p>}<div className="mt-10 flex items-center justify-between border-t border-[#123a35]/10 pt-6"><button onClick={() => setStep(Math.max(0, step - 1))} disabled={step === 0} className="inline-flex items-center gap-2 text-sm font-bold text-[#56716a] disabled:invisible"><ArrowLeft className="h-4 w-4" /> Back</button><button onClick={next} className="inline-flex items-center gap-2 rounded-full bg-[#123a35] px-5 py-3 text-sm font-bold text-white transition-all hover:bg-[#1d514a] active:scale-[.97]">{step === labels.length - 1 ? <>Forge my trip <Sparkles className="h-4 w-4 text-[#ffb34b]" /></> : <>Continue <ArrowRight className="h-4 w-4 text-[#ffb34b]" /></>}</button></div></div></section></main></div>;
}
function LinkBack() { return <a href="/" className="inline-flex items-center gap-2 text-sm font-bold text-[#486962] transition-colors hover:text-[#e6651b]"><ArrowLeft className="h-4 w-4" /> Back to TripsForge</a>; }
